@using Frontend.Models.User

<div class="pp-hero">
    <div class="pp-hero-left">
        <div class="pp-avatar">@Member.FullName[0]</div>
        <div>
            <div class="pp-name">@Member.FullName</div>
            <span class="pp-badge">
                <i class="bi bi-star-fill" aria-hidden="true"></i>
                Premium Medlem
            </span>
        </div>
    </div>
    <div class="pp-hero-right">
        <div class="pp-meta">Medlem siden <span>@Member.SignUpDate.ToString("dd/MM/yyyy")</span></div>
        <div class="pp-meta">Center <span>Aarhus — Nordre</span></div>
        <div class="pp-meta">Næste betaling <span>1. juni 2026</span></div>
    </div>
</div>

@code {
    [Parameter] public Member Member { get; set; } = default!;
}